{% extends "BonPlanBundle:Admin:admin.html.twig" %}
{% block firsta %}

    {% if form is defined %}
        {{ form_start(form) }}
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
{% block thirda %}
    <!--Table-->
    <table class="table table-striped table-responsive-md btn-table">

        <!--Table head-->
        <thead>
        <tr>
            <th>Texte</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
        </thead>
        <!--Table head-->

        <!--Table body-->
        <tbody>
        {% for mot in motsInterdits %}
            <tr>

                <td>{{ mot.texte}}</td>
                <td><a href={{ path('motInterdit_modification',{ 'id':mot.id })}}> <button type="button" class="btn btn-primary btn-rounded btn-sm my-0"  }}>Modifier</button></a></td>
                <td><a href={{ path('motInterdit_suppression',{ 'id':mot    .id })}}> <button type="button" class="btn btn-primary btn-rounded btn-sm my-0"  }}>Supprimer</button></a></td>

            </tr>
        {% endfor %}

        </tbody>
        <!--Table body-->

    </table>
    <!--Table-->

{% endblock %}

{% block fiftha %}

{% endblock %}