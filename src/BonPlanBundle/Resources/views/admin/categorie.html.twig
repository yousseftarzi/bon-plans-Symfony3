{% extends "BonPlanBundle:Admin:admin.html.twig" %}
{% block thirda %}


    <!-- Start your project here-->
    <!--Table-->
    <table class="table table-striped table-responsive-md btn-table">

        <!--Table head-->
        <thead>
        <tr>
            <th>Nom Categorie</th>
            <th>Image</th>
            <th>Modifier</th>
        </tr>
        </thead>
        <!--Table head-->

        <!--Table body-->
        <tbody>
{% for cat in c %}
        <tr>
            <td>{{ cat.nom}}</td>
            <td><img src="{{ asset('uploads/images/' ~ cat.image )}}" width="200" height="200"></td>
            <td><a href={{ path('modifiercategorie_admin',{ 'id':cat.id })}}> <button type="button" class="btn btn-primary btn-rounded btn-sm my-0"  }}>Modifier</button></a></td>
        </tr>
    {% endfor %}

        </tbody>
        <!--Table body-->

    </table>
    <!--Table-->

{% endblock %}