{% extends 'BonPlanBundle:Admin:admin.html.twig' %}
{% block firsta %}

{% if form is defined %}
    {{ form_start(form) }}
    {{ form_end(form) }}
{% endif %}
{% endblock %}
{% block thirda %}


    <!--Table-->
    <table class="table table-striped table-responsive-md btn-table">

        <!--Table head-->
        <thead>
        <tr>
            <th>Nom Critere</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
        </thead>
        <!--Table head-->

        <!--Table body-->
        <tbody>
        {% for crit in c %}
            <tr>
                <td>{{ crit.nom}}</td>
                <td><a href={{ path('modifiercritere_admin',{ 'id':crit.id })}}> <button type="button" class="btn btn-primary btn-rounded btn-sm my-0"  }}>Modifier</button></a></td>
                <td><a href={{ path('supprimercritere_admin',{ 'id':crit.id })}}> <button type="button" class="btn btn-primary btn-rounded btn-sm my-0"  }}>Supprimer</button></a></td>
            </tr>
        {% endfor %}

        </tbody>
        <!--Table body-->

    </table>
    <!--Table-->


{% endblock %}

{% block fiftha %}

{% endblock %}

